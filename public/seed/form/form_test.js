'use strict';

describe('maboApp.form module', function() {

  beforeEach(module('maboApp.form'));

  describe('form controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var formCtrl = $controller('View2Ctrl');
      expect(formCtrl).toBeDefined();
    }));

  });
});