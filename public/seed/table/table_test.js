'use strict';

describe('maboApp.table module', function() {

  beforeEach(module('maboApp.table'));

  describe('table controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var tableCtrl = $controller('View1Ctrl');
      expect(tableCtrl).toBeDefined();
    }));

  });
});